# Data policy

Raw datasets are downloaded into ignored local storage and are never committed. Versioned
manifests record source revisions, hashes, row counts, label mappings and split-integrity results.
Small synthetic fixtures may be committed when they contain no customer or confidential data.

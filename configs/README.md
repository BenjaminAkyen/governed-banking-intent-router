# Configuration policy

Versioned experiment and routing configurations will live here. Thresholds and risk mappings must
not be embedded as unexplained constants in notebooks or API code.

Module 2 will add the dataset configuration. Later modules will add model and routing policies.
